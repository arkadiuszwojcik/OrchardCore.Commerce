<Project>
  <PropertyGroup>
    <!-- Define a base directory (relative or absolute) -->
    <OrchardCoreCommercePath>$(MSBuildThisFileDirectory)..\..\</OrchardCoreCommercePath>
  </PropertyGroup>
</Project>